class Aoc2023::One < Aoc2023::Solution
  def parse_input(input)
    input
    #groups = InputParsers.groups(input)
    ##groups = groups.map do |group|
    #  group.map(&.to_i32)
    #end
  end

  def part1(input)
    #input.map(&.sum).max
  end

  def part2(input)
    #input.map(&.sum).sort.reverse.first(3).sum
  end
end
